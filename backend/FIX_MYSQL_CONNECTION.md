# 🔧 แก้ปัญหา MySQL Connection Error

## ❌ Error: Access denied for user 'root'@'localhost'

### วิธีแก้ปัญหา:

## 📋 วิธีที่ 1: ตรวจสอบรหัสผ่าน MySQL

### ใช้ MySQL Workbench:

1. เปิด **MySQL Workbench**
2. คลิกที่ connection ของคุณ
3. ลองเข้าด้วยรหัสผ่านที่คุณคิดว่าถูกต้อง
4. ถ้าเข้าได้ = รหัสผ่านถูกต้อง ให้ใช้รหัสผ่านนั้นใน `config.js`

### ถ้าเข้า MySQL Workbench ได้:

รันคำสั่งนี้ใน MySQL Workbench เพื่อดูข้อมูล user:

```sql
SELECT user, host FROM mysql.user WHERE user = 'root';
```

---

## 🔑 วิธีที่ 2: รีเซ็ตรหัสผ่าน MySQL (ถ้าจำไม่ได้)

### สำหรับ Windows:

1. **หยุด MySQL Service:**
   - กด `Win + R` พิมพ์ `services.msc`
   - หา `MySQL` service
   - คลิกขวา > Stop

2. **เริ่ม MySQL ในโหมด skip-grant-tables:**
   
   เปิด Command Prompt **as Administrator** และรัน:
   
   ```cmd
   "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld.exe" --skip-grant-tables --shared-memory
   ```

3. **เปิด Command Prompt หน้าต่างใหม่** (as Administrator) และรัน:

   ```cmd
   "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root
   ```

4. **เปลี่ยนรหัสผ่าน:**

   ```sql
   FLUSH PRIVILEGES;
   ALTER USER 'root'@'localhost' IDENTIFIED BY '12345678';
   FLUSH PRIVILEGES;
   EXIT;
   ```

5. **ปิด MySQL และเปิดใหม่แบบปกติ:**
   - ปิดหน้าต่าง Command Prompt ทั้งหมด
   - เปิด `services.msc`
   - Start MySQL service

---

## 👤 วิธีที่ 3: สร้าง User ใหม่ (แนะนำ)

### ใน MySQL Workbench รันคำสั่ง:

```sql
-- สร้าง user ใหม่สำหรับแอป
CREATE USER 'humans_app'@'localhost' IDENTIFIED BY '12345678';

-- ให้สิทธิ์กับ database humans
GRANT ALL PRIVILEGES ON humans.* TO 'humans_app'@'localhost';

-- รีเฟรชสิทธิ์
FLUSH PRIVILEGES;
```

### แล้วแก้ไขใน `config.js`:

```javascript
db: {
  host: 'localhost',
  user: 'humans_app',      // เปลี่ยนเป็น user ใหม่
  password: '12345678',     // รหัสผ่านที่ตั้งไว้
  database: 'humans',
  port: 3306,
}
```

---

## 🆓 วิธีที่ 4: ลองใช้ไม่มีรหัสผ่าน

บาง MySQL installation ไม่มีรหัสผ่านสำหรับ root

### แก้ไขใน `config.js`:

```javascript
password: process.env.DB_PASSWORD || '',  // ค่าว่าง
```

---

## ✅ วิธีที่ 5: ตรวจสอบใน MySQL Workbench

1. เปิด **MySQL Workbench**
2. คลิกที่ **Server** > **Users and Privileges**
3. เลือก user `root`
4. ดูที่แท็บ **Authentication Type**
5. ตรวจสอบว่ามี password หรือไม่

---

## 🧪 ทดสอบหลังแก้ไข

1. แก้ไขรหัสผ่านใน `backend/config.js`
2. Restart backend server:

```bash
# กด Ctrl+C ใน terminal ที่รัน backend
npm start
```

3. ควรเห็น:

```
🚀 Humans HR Backend running on http://localhost:3000
📊 Database: humans@localhost:3306
✅ Connected to MySQL Database
```

---

## 📝 สรุปขั้นตอน Quick Fix:

### วิธีที่เร็วที่สุด (ถ้าเข้า MySQL Workbench ได้):

1. เปิด MySQL Workbench
2. รันคำสั่ง:

```sql
-- ตรวจสอบว่ามี database humans หรือยัง
CREATE DATABASE IF NOT EXISTS humans;

-- สร้าง user ใหม่
CREATE USER IF NOT EXISTS 'humans_app'@'localhost' IDENTIFIED BY '12345678';
GRANT ALL PRIVILEGES ON humans.* TO 'humans_app'@'localhost';
FLUSH PRIVILEGES;
```

3. แก้ไข `backend/config.js`:

```javascript
db: {
  host: 'localhost',
  user: 'humans_app',    // ใช้ user ใหม่
  password: '12345678',
  database: 'humans',
  port: 3306,
}
```

4. Restart backend:

```bash
npm start
```

---

## 🆘 ถ้ายังแก้ไม่ได้

ส่งข้อมูลเหล่านี้มาให้ช่วย:

1. เวอร์ชัน MySQL ที่ใช้
2. เข้า MySQL Workbench ได้หรือไม่
3. ใช้รหัสผ่านอะไรใน MySQL Workbench
4. Error message ที่ได้จาก backend

---

## 🔐 ความปลอดภัย

**สำคัญ!** ในการใช้งานจริง:

- ✅ ไม่ควรใช้ root user สำหรับแอป
- ✅ สร้าง user แยกสำหรับแต่ละแอป
- ✅ ให้สิทธิ์เฉพาะ database ที่จำเป็น
- ✅ ใช้รหัสผ่านที่แข็งแรง
- ✅ เก็บรหัสผ่านใน Environment Variables

