# คู่มือการตั้งค่า Role Manager

## ภาพรวม
ระบบใช้ `role = 'manager'` ในตาราง `login` เพื่อระบุหัวหน้าแผนก

> 📖 **ดู Flow การทำงานแบบละเอียด**: [LEAVE_APPROVAL_FLOW.md](./LEAVE_APPROVAL_FLOW.md)

## ขั้นตอนการตั้งค่า

### 1. แก้ไข Database Schema

รัน SQL script:
```bash
mysql -u root -p humans < backend/add_manager_role.sql
```

หรือรัน SQL โดยตรง:
```sql
USE humans;

-- แก้ไข ENUM ของ role ให้รองรับ 'manager'
ALTER TABLE login 
MODIFY COLUMN role ENUM('admin', 'employee', 'manager') DEFAULT 'employee';
```

### 2. ตั้งค่าให้ User เป็น Manager

#### วิธีที่ 1: ผ่านหน้า Admin Dashboard (แนะนำ)
1. Login ด้วย user ที่เป็น admin
2. ไปที่ Admin Dashboard → จัดการพนักงาน
3. กดแก้ไขพนักงานที่ต้องการ
4. เลือก Role เป็น "Manager"
5. บันทึก

#### วิธีที่ 2: ผ่าน SQL
```sql
USE humans;

-- ตั้งค่าให้ user เป็น manager
UPDATE login 
SET role = 'manager' 
WHERE user_id = 5;  -- เปลี่ยน user_id ตามต้องการ
```

### 3. ตรวจสอบข้อมูล

```sql
-- ตรวจสอบ user ที่เป็น manager
SELECT 
  l.user_id,
  l.username,
  l.email,
  l.role,
  CONCAT(e.first_name, ' ', e.last_name) as employee_name,
  e.department
FROM login l
LEFT JOIN employees e ON l.user_id = e.user_id
WHERE l.role = 'manager';
```

## การทำงานของระบบ

### Role ที่รองรับ:
- **admin**: ผู้ดูแลระบบ - เห็นข้อมูลทั้งหมด
- **manager**: หัวหน้าแผนก - เห็นเฉพาะข้อมูลของแผนกตัวเอง
- **employee**: พนักงานทั่วไป - ไม่มีสิทธิ์อนุมัติการลา

### สำหรับ Manager:
- ✅ เห็นการ์ด "หัวหน้าแผนก" ในหน้า Home
- ✅ สามารถเข้าถึงหน้า "อนุมัติการลา" ได้
- ✅ เห็นเฉพาะวันลาของคนในแผนกเดียวกัน
- ✅ สามารถอนุมัติ/ปฏิเสธการลาได้

### สำหรับ Employee:
- ❌ ไม่เห็นการ์ด "หัวหน้าแผนก"
- ❌ ไม่สามารถเข้าถึงหน้า "อนุมัติการลา" ได้

### สำหรับ Admin:
- ❌ ไม่เห็นการ์ด "หัวหน้าแผนก" ในหน้า Home (ใช้ Admin Dashboard)
- ✅ สามารถเข้าถึงหน้า "อนุมัติการลา" ผ่าน Admin Dashboard ได้
- ✅ เห็นวันลาของทุกคน

## หมายเหตุ
- Manager ต้องมี `department` ที่ถูกต้องในตาราง `employees`
- ระบบจะกรองวันลาให้เห็นเฉพาะคนในแผนกเดียวกัน
- ถ้า Manager ไม่มี `department` จะไม่เห็นวันลาใดๆ

