# 🚀 แก้ปัญหา Backend ไม่เชื่อมต่อ MySQL

## ✅ ทำตามขั้นตอนนี้:

### 1. เปิด MySQL Workbench

เชื่อมต่อเข้า MySQL ด้วยรหัสผ่านปกติของคุณ

### 2. รันคำสั่ง SQL นี้

คัดลอกโค้ดด้านล่างทั้งหมด แล้ววางใน MySQL Workbench แล้วกด Execute (⚡):

```sql
-- สร้าง database
CREATE DATABASE IF NOT EXISTS humans;

-- สร้าง user ใหม่สำหรับแอป
CREATE USER IF NOT EXISTS 'humans_app'@'localhost' IDENTIFIED BY '12345678';

-- ให้สิทธิ์เข้าถึง database humans
GRANT ALL PRIVILEGES ON humans.* TO 'humans_app'@'localhost';

-- รีเฟรชสิทธิ์
FLUSH PRIVILEGES;

-- ตรวจสอบว่าสร้างสำเร็จ
SELECT 'User created successfully!' AS Status;
```

### 3. Restart Backend Server

ใน Terminal ที่รัน backend:
1. กด `Ctrl + C` เพื่อหยุด server
2. รันใหม่: `npm start`

### 4. ผลลัพธ์ที่คาดหวัง

ควรเห็นข้อความนี้:

```
🚀 Humans HR Backend running on http://localhost:3000
📊 Database: humans@localhost:3306
✅ Connected to MySQL Database
```

---

## 📝 หมายเหตุ:

- ✅ ฉันได้แก้ไข `config.js` ให้ใช้ user `humans_app` แทน `root` แล้ว
- ✅ ปลอดภัยกว่าการใช้ root user
- ✅ แก้ปัญหา Access Denied ได้

---

## ❓ ถ้ายังไม่ได้

แจ้งข้อความ error ที่ได้มาได้เลยครับ

